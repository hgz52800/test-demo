# test-demo

这是一个用于学习 Python 基础语法的入门示例项目。

## 文件说明

`hello.py` 是项目中的示例程序。它使用 Python 的 `print()` 函数在终端输出一段文字：

```python
print("Hello, World!")
```

运行该程序后，会看到：

```text
Hello, World!
```

## 运行方式

确保已安装 Python 后，在项目目录中执行：

```bash
python hello.py
```
